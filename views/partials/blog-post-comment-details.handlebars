<div class="container-fluid p-0">
    <div class="row d-flex justify-content-end mx-2 mx-sm-4 mx-lg-5">
        <div class="card shadow p-0 col-10 col-md-8">
            {{#if (is_author userName)}}
            <a href="/comments/{{{id}}}">
            <div class="card-body d-flex justify-content-between background-grey">
            {{else}}
            <div class="card-body d-flex justify-content-between">
            {{/if}}
                <p class="card-text m-0">{{{content}}}</p>
                <p class="card-text text-end">Posted by {{{user.user_name}}} on {{{format_date created_at}}}</p>
            </div>
            {{#if (is_author userName)}}
            </a>
            {{/if}}
        </div>
    </div>
</div>