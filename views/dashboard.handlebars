<div class="container-fluid mb-5">
    {{#each userBlogPosts as |blogPost|}}
    {{> user-blog-post-button blogPost loggedIn=loggedIn userName=userName}}
    {{/each}}
</div>
<div class="container-fluid">
    <div class="row mx-3 mx-lg-4 mt-5 mb-0 d-flex justify-content-center">
        <button class="btn btn-lg shadow btn-custom col-2">+ New Post</button>
    </div>
</div>